# Intentionally empty. Compose bind-mounts this over ignored local env files so
# the source workspace mount cannot bypass Podman Secret delivery.
#
# It sits at the repository root, beside the `compose.yaml` that names it, so
# that `.devcontainer/` holds nothing but `devcontainer.json` and its lock file.
