name: CI

on:
  push:
    branches: [develop, 'main']
  pull_request:
    branches: [develop, 'main']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: '25'
          cache: true
      - run: vp install
      - run: vp check
      - run: vp test
      - run: pnpm run build:apex:check
