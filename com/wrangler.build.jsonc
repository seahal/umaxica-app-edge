/**
 * Wrangler config used only during dev/build (Vite plugin).
 * Points to a source entry that exists pre-build to satisfy plugin validation.
 */
{
	"$schema": "../node_modules/wrangler/config-schema.json",
	"name": "umaxica-edge-com-dev",
	"compatibility_date": "2025-04-04",
	"main": "./workers/app.ts",
	"observability": { "enabled": true }
}
