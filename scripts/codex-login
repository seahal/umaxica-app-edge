#!/usr/bin/env bash
set -euo pipefail

secret=/run/secrets/dev_codex_api_key
if [[ ! -f ${secret} ]]; then
  echo 'BLOCKED: dev_codex_api_key is not mounted; start with --credentials.' >&2
  exit 2
fi
exec codex login --with-api-key <"${secret}"
