#!/usr/bin/env bash
set -euo pipefail

if [[ ${EUID} -eq 0 || -n ${SUDO_UID:-} ]]; then
  echo 'ERROR: build with rootless Podman, never root or sudo.' >&2
  exit 1
fi
exec podman build --file Containerfile --tag umaxica-apps-edge-dev:local .
