name: "edge"
services:
  main:
    build:
      context: .
      target: development
    working_dir: /home/edge/main
    volumes:
      - type: bind
        source: .
        target: /home/edge/main
      - type: volume
        source: node-volume
        target: /home/edge/main/node_modules
        volume:
          nocopy: true
      - type: volume
        source: bun-volume
        target: /home/edge/main/.bun
        volume:
          nocopy: true
    ports:
      - '5130:5130'
      - '5131:5131'
      - '5132:5132'
      - '5133:5133'
    command: sleep infinity
    tty: true
    stdin_open: true
    init: true
    user: "${UID:-1000}:${GID:-1000}"
volumes:
  bun-volume:
  node-volume:
