name: 'edge'
services:
  main:
    build: .
    restart: always
    tty: true
    stdin_open: true
    ports:
      - "4444:4000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/main:delegated
    command: bun run server