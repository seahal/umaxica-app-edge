{
  "name": "umaxica-apps-edge-app-docs",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "server": "astro dev --port 5403",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler versions upload --config dist/server/wrangler.json",
    "cf-typegen": "wrangler types",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.3",
    "astro": "^6.0.8"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
