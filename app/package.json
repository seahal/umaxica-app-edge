{
	"name": "umaxica-edge-app",
	"version": "0.1.0",
	"description": "code for xx.umaxica.com",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "react-router build",
		"cf-typegen": "bunx wrangler types",
		"deploy": "bun run build && bunx wrangler deploy",
		"server": "react-router dev",
		"postinstall": "bun run cf-typegen || true",
		"preview": "bun run build && vite preview",
		"test": "vitest",
		"typecheck": "bun run cf-typegen && react-router typegen && bunx tsc -b",
		"format": "../node_modules/.bin/biome format src test --write"
	}
}
