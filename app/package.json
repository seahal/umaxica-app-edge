{
  "name": "umaxica-app-apex-staging-app",
  "type": "module",
  "scripts": {
    "server": "wrangler dev --ip 0.0.0.0 --port 8781",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.11.9"
  }
}
