{
	"name": "umaxica-app-edge-dev",
	"version": "0.1.0",
	"description": "code for xx.umaxica.com",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "react-router build",
		"cf-typegen": "bunx wrangler types",
		"typegen": "react-router typegen",
		"deploy": "bun run build && bunx wrangler deploy",
		"server": "react-router dev",
		"postinstall": "bun run cf-typegen || true",
		"type": "bunx tsgo --noEmit"
	}
}
