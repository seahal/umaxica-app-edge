{
  "name": "umaxica-apps-edge-org-jump",
  "type": "module",
  "scripts": {
    "server": "wrangler dev",
    "build": "pnpm run type",
    "deploy": "pnpm run build && wrangler deploy",
    "deploy:upload": "pnpm run build && wrangler versions upload",
    "deploy:promote": "wrangler versions deploy --yes",
    "cf-typegen": "pnpm wrangler types --env-interface CloudflareBindings",
    "type": "vp exec tsc --noEmit"
  },
  "dependencies": {
    "hono": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
