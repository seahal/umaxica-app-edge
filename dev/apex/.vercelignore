# Vercel falls back to .gitignore when this file is absent, and .gitignore
# excludes the generated public/style.css — which would ship this unit without
# its stylesheet. Listing the uploads explicitly instead keeps the compiled
# CSS in the deployment; `deploy` regenerates it immediately beforehand.
node_modules
.wrangler
dist
test-results
playwright-report
coverage
