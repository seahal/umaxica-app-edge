{
  "name": "umaxica-apps-edge-dev-acme",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 5502",
    "test:e2e": "playwright test",
    "build": "next build",
    "start": "next start --port 5502",
    "deploy": "vercel deploy --prod",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "test:api": "node api/run.mjs",
    "knip": "knip --treat-config-hints-as-errors",
    "lint": "oxlint --report-unused-disable-directives .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "check": "pnpm run check:static && pnpm run test",
    "lint:types": "oxlint --type-aware --report-unused-disable-directives .",
    "lint:fix": "oxlint --type-aware --fix .",
    "check:static": "pnpm run format:check && pnpm run lint && pnpm run lint:types && pnpm run typecheck && pnpm run knip",
    "check:size": "size-limit"
  },
  "dependencies": {
    "@sentry/nextjs": "^10.51.0",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@orangeopensource/hurl": "catalog:",
    "@playwright/test": "catalog:",
    "@size-limit/file": "catalog:",
    "@tailwindcss/postcss": "catalog:",
    "@testing-library/jest-dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "happy-dom": "catalog:",
    "knip": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "size-limit": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vercel": "catalog:",
    "vitest": "catalog:"
  }
}
