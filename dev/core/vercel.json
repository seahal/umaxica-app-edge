{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --filter ./dev/core... --ignore-scripts",
  "buildCommand": "pnpm run build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/api/$1"
    }
  ]
}
